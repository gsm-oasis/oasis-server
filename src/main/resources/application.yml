spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/realoasis?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: pynn2409!

  jpa:
    hibernate:
      ddl-auto: create

jwt:
  secret:
    key: ${JWT_KEY}